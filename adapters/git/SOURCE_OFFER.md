<!-- SPDX-License-Identifier: GPL-2.0-only -->

# Corresponding source

Before any network deployment, publish the complete corresponding source for
the exact FileBelt revision, this GPL adapter workspace, the linked Apache
revision-protocol source/generated output, Git `2.55.0` source, build recipe,
patches (if any), notices, SBOM, and image provenance. This repository state
does not claim an admitted or published adapter image.
