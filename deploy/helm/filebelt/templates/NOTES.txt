{{/* SPDX-License-Identifier: Apache-2.0 */}}
FileBelt Phase 3 rendered a Kubernetes {{ .Capabilities.KubeVersion.Version }} release.

Mode: {{ if .Values.deployment.quiesced }}QUIESCED (all application replicas are zero){{ else }}serving{{ end }}
Administrative operation: {{ .Values.operation.type }}

Before enabling traffic, verify that:
  * all five image digests are release digests, not the all-zero sentinel;
  * external PostgreSQL, OIDC egress, DNS, ingress, monitoring, and optional
    Iggy/OTLP NetworkPolicy peers match this namespace;
  * every referenced Secret and the existing RWX PVC are operator-managed;
  * migration, owner-applied grants, grant verification, bootstrap, and storage
    probe evidence has been retained; and
  * OxiBelt includes the reviewed outbound client-certificate capability.

The chart creates no database, credential, PVC, ingress, controller, or RBAC
permission. Read deploy/helm/filebelt/README.md before changing quiesced mode or
running an explicit Job.
