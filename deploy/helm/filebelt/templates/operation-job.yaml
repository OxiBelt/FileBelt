{{/* SPDX-License-Identifier: Apache-2.0 */}}
{{- if ne .Values.operation.type "none" }}
{{- $type := .Values.operation.type -}}
{{- $storageOperation := has $type (list "storage-probe" "storage-scrub-start" "storage-scrub-status" "storage-scrub-verify") -}}
{{- $payloadOperation := eq $type "storage-probe" -}}
{{- $recoveryOperation := has $type (list "recovery-checkpoint" "recovery-verify") -}}
{{- $securityDescendantSharesOperation := has $type (list "security-descendant-shares-status" "security-descendant-shares-repair" "security-descendant-shares-verify" "security-descendant-shares-activate") -}}
{{- $keysetInventoryOperation := or $recoveryOperation (eq $type "keys-audit") -}}
{{- $databaseSecret := "" -}}
{{- if eq $type "migrate" -}}
{{- $databaseSecret = .Values.secrets.migratorDatabase.name -}}
{{- else if eq $type "bootstrap" -}}
{{- $databaseSecret = .Values.secrets.migratorDatabase.name -}}
{{- else if eq $type "verify-grants" -}}
{{- $databaseSecret = .Values.secrets.migratorDatabase.name -}}
{{- else if eq $type "audit-export" -}}
{{- $databaseSecret = .Values.secrets.auditDatabase.name -}}
{{- else if has $type (list "storage-scrub-start" "storage-scrub-status" "storage-scrub-verify") -}}
{{- $databaseSecret = .Values.secrets.maintenanceDatabase.name -}}
{{- else if $securityDescendantSharesOperation -}}
{{- $databaseSecret = .Values.secrets.recoveryDatabase.name -}}
{{- else if not (has $type (list "keys-audit" "storage-probe")) -}}
{{- $databaseSecret = .Values.secrets.recoveryDatabase.name -}}
{{- end -}}
{{- $databaseKey := "" -}}
{{- if eq $type "migrate" -}}
{{- $databaseKey = .Values.secrets.migratorDatabase.key -}}
{{- else if eq $type "bootstrap" -}}
{{- $databaseKey = .Values.secrets.migratorDatabase.key -}}
{{- else if eq $type "verify-grants" -}}
{{- $databaseKey = .Values.secrets.migratorDatabase.key -}}
{{- else if eq $type "audit-export" -}}
{{- $databaseKey = .Values.secrets.auditDatabase.key -}}
{{- else if has $type (list "storage-scrub-start" "storage-scrub-status" "storage-scrub-verify") -}}
{{- $databaseKey = .Values.secrets.maintenanceDatabase.key -}}
{{- else if $securityDescendantSharesOperation -}}
{{- $databaseKey = .Values.secrets.recoveryDatabase.key -}}
{{- else if ne $type "storage-probe" -}}
{{- $databaseKey = .Values.secrets.recoveryDatabase.key -}}
{{- end -}}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ printf "%s-%s-%s" (include "filebelt.name" .) $type (.Values.operation.operationId | trunc 12) | trunc 63 | trimSuffix "-" }}
  labels:
{{ include "filebelt.componentLabels" (dict "root" . "component" "tools-operation") | indent 4 }}
    filebelt.dev/operation-type: {{ $type | quote }}
  annotations:
    filebelt.dev/operation-id: {{ .Values.operation.operationId | quote }}
spec:
  backoffLimit: 0
  completions: 1
  parallelism: 1
  activeDeadlineSeconds: 21600
  template:
    metadata:
      labels:
{{ include "filebelt.componentLabels" (dict "root" . "component" "tools-operation") | indent 8 }}
        filebelt.dev/operation-type: {{ $type | quote }}
      annotations:
        checksum/config: {{ include "filebelt.renderedFilebeltConfiguration" . | sha256sum | quote }}
        checksum/secret-generations: {{ include "filebelt.secretGenerationDigest" . | quote }}
        filebelt.dev/operation-id: {{ .Values.operation.operationId | quote }}
    spec:
      serviceAccountName: {{ include "filebelt.name" . }}-tools
      automountServiceAccountToken: false
      enableServiceLinks: false
      restartPolicy: Never
      terminationGracePeriodSeconds: 90
      securityContext:
{{ include "filebelt.podSecurityContext" . | indent 8 }}
      containers:
        - name: operation
          image: {{ include "filebelt.image" (dict "root" . "role" "filebelt-tools") }}
          imagePullPolicy: IfNotPresent
          args:
          {{- if eq $type "migrate" }}
            - database
            - migrate
          {{- else if eq $type "bootstrap" }}
            - tenant
            - bootstrap
          {{- else if eq $type "verify-grants" }}
            - database
            - verify-grants
          {{- else if eq $type "storage-probe" }}
            - storage
            - probe
          {{- else if eq $type "keys-audit" }}
            - keys
            - audit
          {{- else if eq $type "storage-scrub-start" }}
            - storage
            - scrub
            - start
            - --run-id
            - {{ .Values.operation.operationId | quote }}
            {{- if .Values.operation.payloadId }}
            - --payload-id
            - {{ .Values.operation.payloadId | quote }}
            {{- else }}
            - --confirm-tenant
            - {{ .Values.operation.tenantSlugConfirmation | quote }}
            {{- end }}
            - --batch-size
            - "1000"
          {{- else if eq $type "storage-scrub-status" }}
            - storage
            - scrub
            - status
            - --run-id
            - {{ .Values.operation.operationId | quote }}
            {{- if .Values.operation.payloadId }}
            - --payload-id
            - {{ .Values.operation.payloadId | quote }}
            {{- end }}
          {{- else if eq $type "storage-scrub-verify" }}
            - storage
            - scrub
            - verify
            - --run-id
            - {{ .Values.operation.operationId | quote }}
            {{- if .Values.operation.payloadId }}
            - --payload-id
            - {{ .Values.operation.payloadId | quote }}
            {{- end }}
          {{- else if eq $type "recovery-checkpoint" }}
            - recovery
            - checkpoint
          {{- else if eq $type "recovery-verify" }}
            - recovery
            - verify
            - --checkpoint
            - /run/input/checkpoint.json
          {{- else if eq $type "audit-export" }}
            - audit
            - export
          {{- else if $securityDescendantSharesOperation }}
            - security
            - descendant-shares
            - {{ trimPrefix "security-descendant-shares-" $type }}
            - --operation-id
            - {{ .Values.operation.operationId | quote }}
            {{- if ne $type "security-descendant-shares-status" }}
            - --confirm-tenant
            - {{ .Values.operation.tenantSlugConfirmation | quote }}
            - --actor-principal-id
            - {{ .Values.operation.actorPrincipalId | quote }}
            {{- end }}
          {{- end }}
            - --config
            - /etc/filebelt/filebelt.toml
          {{- range .Values.operation.args }}
            - {{ . | quote }}
          {{- end }}
          {{- if $databaseSecret }}
          env:
            - {name: FILEBELT_DATABASE_URL_FILE, value: /run/secrets/database-url}
          {{- end }}
          securityContext:
{{ include "filebelt.containerSecurityContext" . | indent 12 }}
          resources:
{{ toYaml (ternary .Values.workloads.operation.resources.storage .Values.workloads.operation.resources.database $storageOperation) | indent 12 }}
          volumeMounts:
            - {name: filebelt-config, mountPath: /etc/filebelt, readOnly: true}
            {{- if $databaseSecret }}
            - {name: database, mountPath: /run/secrets/database-url, subPath: database-url, readOnly: true}
            {{- end }}
            {{- if $payloadOperation }}
            - {name: payloads, mountPath: {{ .Values.storage.mountPath }}}
            {{- end }}
            {{- if eq $type "recovery-verify" }}
            - {name: checkpoint, mountPath: /run/input, readOnly: true}
            {{- end }}
            {{- if $keysetInventoryOperation }}
            - {name: api-storage-capability-public, mountPath: /run/secrets/api-storage-capability-public-keyset, subPath: public-keyset, readOnly: true}
            - {name: media-storage-capability-public, mountPath: /run/secrets/media-storage-capability-public-keyset, subPath: public-keyset, readOnly: true}
            {{- if .Values.collaboration.enabled }}
            - {name: api-collaboration-grant-capability-public, mountPath: /run/secrets/api-collaboration-grant-capability-public-keyset, subPath: public-keyset, readOnly: true}
            - {name: collaboration-storage-capability-public, mountPath: /run/secrets/collaboration-storage-capability-public-keyset, subPath: public-keyset, readOnly: true}
            {{- end }}
            {{- if .Values.mcp.enabled }}
            - {name: api-mcp-delegation-capability-public, mountPath: /run/secrets/api-mcp-delegation-capability-public-keyset, subPath: public-keyset, readOnly: true}
            {{- end }}
            {{- if .Values.documents.enabled }}
            - {name: document-storage-capability-public, mountPath: /run/secrets/document-storage-capability-public-keyset, subPath: public-keyset, readOnly: true}
            {{- end }}
            {{- if .Values.mounts.enabled }}
            - {name: mount-storage-capability-public, mountPath: /run/secrets/mount-storage-capability-public-keyset, subPath: public-keyset, readOnly: true}
            {{- end }}
            {{- end }}
            - {name: tmp, mountPath: /tmp}
      volumes:
{{ include "filebelt.filebeltConfigVolume" . | indent 8 }}
        {{- if $databaseSecret }}
        - name: database
          secret:
            secretName: {{ $databaseSecret }}
            defaultMode: 0440
            items: [{key: {{ $databaseKey }}, path: database-url}]
        {{- end }}
        {{- if $payloadOperation }}
        - name: payloads
          persistentVolumeClaim: {claimName: {{ .Values.storage.existingClaim }}}
        {{- end }}
        {{- if eq $type "recovery-verify" }}
        - name: checkpoint
          secret:
            secretName: {{ .Values.operation.checkpoint.secretName }}
            defaultMode: 0440
            items: [{key: {{ .Values.operation.checkpoint.key }}, path: checkpoint.json}]
        {{- end }}
        {{- if $keysetInventoryOperation }}
        - name: api-storage-capability-public
          secret:
            secretName: {{ .Values.secrets.apiStorageCapabilityPublicKeyset.name }}
            defaultMode: 0440
            items: [{key: {{ .Values.secrets.apiStorageCapabilityPublicKeyset.key }}, path: public-keyset}]
        - name: media-storage-capability-public
          secret:
            secretName: {{ .Values.secrets.mediaStorageCapabilityPublicKeyset.name }}
            defaultMode: 0440
            items: [{key: {{ .Values.secrets.mediaStorageCapabilityPublicKeyset.key }}, path: public-keyset}]
        {{- if .Values.collaboration.enabled }}
        - name: api-collaboration-grant-capability-public
          secret:
            secretName: {{ .Values.secrets.apiCollaborationGrantCapabilityPublicKeyset.name }}
            defaultMode: 0440
            items: [{key: {{ .Values.secrets.apiCollaborationGrantCapabilityPublicKeyset.key }}, path: public-keyset}]
        - name: collaboration-storage-capability-public
          secret:
            secretName: {{ .Values.secrets.collaborationStorageCapabilityPublicKeyset.name }}
            defaultMode: 0440
            items: [{key: {{ .Values.secrets.collaborationStorageCapabilityPublicKeyset.key }}, path: public-keyset}]
        {{- end }}
        {{- if .Values.mcp.enabled }}
        - name: api-mcp-delegation-capability-public
          secret:
            secretName: {{ .Values.secrets.apiMcpDelegationCapabilityPublicKeyset.name }}
            defaultMode: 0440
            items: [{key: {{ .Values.secrets.apiMcpDelegationCapabilityPublicKeyset.key }}, path: public-keyset}]
        {{- end }}
        {{- if .Values.documents.enabled }}
        - name: document-storage-capability-public
          secret:
            secretName: {{ .Values.secrets.documentStorageCapabilityPublicKeyset.name }}
            defaultMode: 0440
            items: [{key: {{ .Values.secrets.documentStorageCapabilityPublicKeyset.key }}, path: public-keyset}]
        {{- end }}
        {{- if .Values.mounts.enabled }}
        - name: mount-storage-capability-public
          secret:
            secretName: {{ .Values.secrets.mountStorageCapabilityPublicKeyset.name }}
            defaultMode: 0440
            items: [{key: {{ .Values.secrets.mountStorageCapabilityPublicKeyset.key }}, path: public-keyset}]
        {{- end }}
        {{- end }}
{{ include "filebelt.tmpVolume" . | indent 8 }}
{{- end }}
