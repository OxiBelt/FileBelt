<!-- SPDX-License-Identifier: Apache-2.0 -->

# FileBelt protocols

Future protocol-neutral Protobuf `proto3` schemas live under
`protocol/<domain>/v1/` and use `filebelt.<domain>.v1` packages. No schema or
transport is defined in Phase 0. Generated output is committed only after its
source schema and generator are accepted.
