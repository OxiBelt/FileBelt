# SPDX-License-Identifier: Apache-2.0

version: v2
clean: true
plugins:
  # protoc-gen-prost v0.5.0 uses Prost 0.14 and is pinned to the exact BSR
  # packaging revision so regeneration cannot silently change the toolchain.
  - remote: buf.build/community/neoeinstein-prost:v0.5.0
    revision: 1
    out: generated/rust
