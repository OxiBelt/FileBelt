# SPDX-License-Identifier: Apache-2.0

version: v2
modules:
  - path: .
lint:
  use:
    - STANDARD
  except:
    # The public source tree is protocol/<domain>/v1 while package names retain
    # the repository-wide filebelt prefix; see InterfacesAndCapabilities.md.
    - PACKAGE_DIRECTORY_MATCH
breaking:
  use:
    - FILE
